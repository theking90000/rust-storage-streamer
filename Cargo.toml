[package]
name = "s-streamer"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
name = "s_streamer"
path = "src/lib.rs"

[dependencies]
bytes = "1"
tokio = { version = "1", features = ["macros", "rt", "sync", "test-util", "time"] }
