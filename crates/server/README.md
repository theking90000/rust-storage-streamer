# server

Axum HTTP server for `frame-streamer` objects.

Provides a REST API with SQLite-backed catalog, segmented file
upload/download, and AES-256-GCM encrypted storage.

## Usage

```sh
cargo run -p server
```
